# PHPStan Config Immutability (Project Rule)

- Target file: `/var/www/html/_bases/base_techplanner_fila3_mono/laravel/phpstan.neon`
- Status: IMMUTABLE. Do not modify this file via automation or assistants.
- Ownership: Only the user may edit this file manually.

## How to adjust analysis without editing phpstan.neon
- Use CLI flags and explicit paths, e.g.:
  - `./vendor/bin/phpstan analyze Modules/User Modules/Geo --level=9 --no-progress --memory-limit=2G`
  - `./vendor/bin/phpstan analyze Modules --debug --no-progress`
- Run per-module or per-folder to scope analysis.

## Enforcement
- Assistants and scripts MUST NOT patch `phpstan.neon`.
- Prefer per-run options and per-module execution.

## References
- See also `laravel/.ai/guidelines/phpstan-config-immutability.md`.
