<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('clients', function (Blueprint $table) {
            $table->id();
            $table->boolean('is_inactive')->default(false)->index();
            $table->string('company_name', 100)->nullable();
            $table->string('health_authority', 100)->nullable();
            $table->string('fiscal_code', 16)->nullable();
            $table->string('vat_number', 11)->nullable();
            $table->string('company_address', 100)->nullable();
            $table->string('street_address', 100)->nullable();
            $table->string('street_number', 10)->nullable();
            $table->string('province', 2)->nullable();
            $table->string('postal_code', 5)->nullable();
            $table->string('phone', 20)->nullable();
            $table->string('fax', 20)->nullable();
            $table->string('mobile', 20)->nullable();
            $table->string('email')->nullable();
            $table->text('notes')->nullable();
            $table->string('business_type', 100)->nullable();
            $table->timestamps();
            $table->softDeletes();
        });

        Schema::create('devices', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained()->cascadeOnDelete();
            $table->string('device_type')->nullable();
            $table->string('brand', 50)->nullable();
            $table->string('model', 50)->nullable();
            $table->string('headset_serial', 50)->nullable();
            $table->string('tube_serial', 50)->nullable();
            $table->string('power_kv', 20)->nullable();
            $table->string('current_ma', 20)->nullable();
            $table->timestamp('first_verification_date')->nullable();
            $table->text('notes')->nullable();
            $table->timestamps();
            $table->softDeletes();
        });

        Schema::create('medical_directors', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained()->cascadeOnDelete();
            $table->string('last_name', 50)->nullable();
            $table->string('first_name', 50)->nullable();
            $table->string('residence_city', 50)->nullable();
            $table->string('street_address', 100)->nullable();
            $table->string('street_number', 10)->nullable();
            $table->string('province', 2)->nullable();
            $table->string('birth_place', 50)->nullable();
            $table->date('birth_date')->nullable();
            $table->timestamp('start_date')->nullable();
            $table->timestamp('end_date')->nullable();
            $table->timestamps();
            $table->softDeletes();
        });

        Schema::create('legal_representatives', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained()->cascadeOnDelete();
            $table->string('last_name', 50)->nullable();
            $table->string('first_name', 50)->nullable();
            $table->string('birth_place', 50)->nullable();
            $table->date('birth_date')->nullable();
            $table->string('residence_city', 50)->nullable();
            $table->string('street_address', 100)->nullable();
            $table->string('street_number', 10)->nullable();
            $table->string('province', 2)->nullable();
            $table->timestamp('start_date')->nullable();
            $table->timestamp('end_date')->nullable();
            $table->timestamps();
            $table->softDeletes();
        });

        Schema::create('legal_offices', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained()->cascadeOnDelete();
            $table->string('city', 50)->nullable();
            $table->string('street_address', 100)->nullable();
            $table->string('street_number', 10)->nullable();
            $table->string('province', 2)->nullable();
            $table->string('postal_code', 5)->nullable();
            $table->boolean('is_inactive')->default(false);
            $table->timestamps();
            $table->softDeletes();
        });

        Schema::create('device_verifications', function (Blueprint $table) {
            $table->id();
            $table->foreignId('device_id')->constrained()->cascadeOnDelete();
            $table->timestamp('verification_date')->nullable();
            $table->timestamp('next_verification_date')->nullable();
            $table->boolean('is_verified')->default(false);
            $table->decimal('service_cost', 10, 2)->nullable();
            $table->text('notes')->nullable();
            $table->timestamps();
            $table->softDeletes();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('device_verifications');
        Schema::dropIfExists('legal_offices');
        Schema::dropIfExists('legal_representatives');
        Schema::dropIfExists('medical_directors');
        Schema::dropIfExists('devices');
        Schema::dropIfExists('clients');
    }
};
